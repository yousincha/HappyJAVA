public class MyMath {

    //자동생성 - 기본생성자는 생성자가 없을 경우 컴파일할 때 자동으로 생성된다.
    public MyMath(){}
    public static int abs(int x){
        if(x<0)
            return x * -1;
        else
            return x;
    }
}
